export class Kick {
    constructor (
        id?: number,
        exerciseId?: number,
        kickDateTime?: Date,
        kickPower?: number,
        reactionSpeed?: number,
        kickAccuracy?: number  
    ) {}
}